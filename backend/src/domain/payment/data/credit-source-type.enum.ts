/**
 * CreditSourceType
 * None: none
 * Join: join
 * Event: event
 * Reward: reward
 * Charge: charge
 * Refund: refund
 * Cancel: cancel
 * Coupon: coupon
 * Feature: feature
 * Expired: expired
 */
export enum CreditSourceType {
  none = 'none',
  join = 'join',
  event = 'event',
  reward = 'reward',
  charge = 'charge',
  refund = 'refund',
  cancel = 'cancel',
  coupon = 'coupon',
  feature = 'feature',
  expired = 'expired',
}
